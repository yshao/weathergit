def get_time_group():
  trange,file_idx=dc.get_data()
  tdir_idx=get_timestamp_dir()
  
  
  
  l_tdir_idx=aggro_timestamp(trange,file_ind,tdirind)
  
  def get_timestamp_dir():
    dir
    
  def batch_retrieve(file_group):
    for group in file_group:
      for file in group:
        get()
      
    
    
def sync_time(tstamp,l_host):
  for ip in l_host:
    run('date -s %(tstamp)s' % tstamp) 
 